/**
 * Strategy framework.
 *
 * Base class and types for implementing trading strategies.
 */

export { Strategy } from './Strategy'
export type { StrategyClass, StrategyContext, OrderExecutor } from './Strategy'
